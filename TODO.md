# TODO

- [ ] Restructure `case-study.html` to remove duplicated/broken markup and align section order with the provided case-study content (Project Introduction → Problem → Requirements → Process → Research → Market Research → Users → IA → Wireframes → UI Decisions → Final UI → Outcome).
- [ ] Update `case-study.html` text content to match the provided pasted content (no placeholder text).
- [x] Update `case-study.css` hero layout to prevent overlap (responsive images + text font-size reduction at breakpoints).

- [ ] Keep `case-study.js` unchanged unless TOC/IDs require adjustments.
- [ ] Sanity test by running `case-study.html` in browser (manual) and confirming:
  - [ ] No HTML errors
  - [ ] TOC links work
  - [ ] Hero images never overlap left text at common widths.
